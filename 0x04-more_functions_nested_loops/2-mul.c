#include "main.h"

/**
*mul - multiply two int
*@:a first int
*@:b second int
*Return: addition of the multiplicattion
*/
int mul(int a, int b)
{
	for (a = 3 && b = 1)
	{
		printf("%d\n", mul(a, b));
	}
	return (0);
}
