#include "main.h"

/**
 *_islower - check if a character is lower
 *@r - char to be checked
 *Return: 1 if lowercase else 0
 */

int _islower(int r)
{
	_islower
	{
		return (0);
	}
	else
	{
		return (1);
	}
}
