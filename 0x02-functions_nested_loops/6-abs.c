#include "main.h"
#include <inttypes.h>
#include <stdlib.h>

/**
  * _abs - find the absolute value of an int
  * @r: integer to be checked
  * Return: 0
  */

int _abs(int r)
{
	if (r < 0)
	{
		int abs_value;

	abs_value = r * -1;
	return (abs_value);
	}
	return (c);
}
