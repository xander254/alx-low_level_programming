#include <stdio.h>

/**
  *main - This is the entry point of my program
  *printf: A command used to pring to the standard output
  *Return: always 0
  */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
