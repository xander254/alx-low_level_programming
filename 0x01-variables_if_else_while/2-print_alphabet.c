#include <stdio.h>

/**
  * main - the entry point of my algorithm
  * putchar - print values
  * Return: always 0
  */

int main(void)
{
	char x = 97;

	for (x = 97; x <= 122; x++)
	{
		putchar('a');
	}
	putchar('\n');
	return (0);
}
